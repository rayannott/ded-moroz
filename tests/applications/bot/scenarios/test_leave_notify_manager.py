class TestLeaveNotifyManager:
    """Getting notifications when someone leaves the room.

    Given:
    - two registered users: u1 and u2
    - a room created by u1, with u2 as a member
    When:
    - u1 leaves the room
    Then:
    - u2 should receive a notification about u1 leaving (as u1 is the manager)
    """

    pass
